# 多模态融合模型运行步骤

## 项目简介

本项目实现了一个基于多模态融合的网络流量分类模型，结合统计特征模态和文本描述模态进行恶意流量检测。

### 主键体系

项目采用三级主键体系管理数据和模型：

| 主键 | 作用 | 示例 |
|------|------|------|
| `dataset_id` | 标识数据集子集 | `dataset_0.csv`, `dataset_1.csv` |
| `split_id` | 标识同一数据集的不同划分 | `split_data/dataset_0/split_0/` |
| `model_id` | 标识不同的训练结果 | `saved_models/model_0/` |

---

## 运行步骤

### 1. 安装依赖

```bash
pip install torch transformers datasets scikit-learn pandas numpy pytest
```

> 默认已完成，若缺少依赖可执行上述命令。

### 2. 下载预训练模型

下载 BERT 中文模型和 Qwen2.5-1.5B 大语言模型：

```bash
python download_bert.py
python download_qwen.py
```

- **涉及文件**：`download_bert.py`, `download_qwen.py`
- **模型路径**：`./models/bert/`, `./models/qwen2.5-1.5b/`
- **可更换模型**：修改脚本中的模型名称即可

### 3. 数据清洗与预处理

将原始数据集（CSV格式）放入 `data_processing/` 目录，执行数据清洗：

```bash
python data_cleaning.py
```

- **涉及文件**：`data_cleaning.py`
- **输出结果**：`processed_dataset/processed_dataset.csv`
- **处理内容**：缺失值处理、异常值检测、标签编码（BENIGN→0, DDoS→1）

### 4. 提取数据集子集

由于原始数据集样本量过大（20万+），随机抽取部分样本作为子集：

```bash
# 自动递增ID
python extract_subset.py --num_samples 5000

# 指定数据集ID
python extract_subset.py --num_samples 5000 --dataset_id 0
```

- **涉及文件**：`extract_subset.py`
- **参数说明**：
  - `--num_samples`: 提取样本数量（默认5000）
  - `--dataset_id`: 数据集ID（默认自动递增）
  - `--random_state`: 随机种子（默认42）
- **输出结果**：
  - `processed_dataset/dataset_X.csv`（子集数据）
  - `processed_dataset/subset_X_scaled_features.npy`（标准化特征）
  - `processed_dataset/subset_X_labels.npy`（标签）
  - `processed_dataset/subset_X_scaler.npy`（标准化器参数）
- **日志记录**：自动生成 `logs/subset/log_X.json`

### 5. 模态分离与数据集划分

将数据集转换为统计特征模态和文本描述模态，并划分为训练集和测试集：

```bash
# 使用默认ID
python split_modality.py

# 指定数据集ID和划分ID
python split_modality.py --dataset_id 0 --split_id 0
```

- **涉及文件**：`split_modality.py`
- **参数说明**：
  - `--dataset_id`: 数据集ID（默认0）
  - `--split_id`: 划分ID（默认自动递增）
  - `--test_size`: 测试集比例（默认0.2）
  - `--random_state`: 随机种子（默认42）
- **输出结果**：`split_data/dataset_X/split_Y/`
  - `train.npz`（训练集：统计特征 + BERT嵌入 + 标签）
  - `test.npz`（测试集：统计特征 + BERT嵌入 + 标签）
  - `train_data.csv`（训练集原始数据）
  - `test_data.csv`（测试集原始数据）
  - `train_scaler.npy`（训练集标准化器参数）
- **日志记录**：自动生成 `logs/split/log_X.json`

### 6. 模型训练

使用划分好的训练集进行模型训练：

```bash
# 默认训练
python train.py

# 指定数据和模型ID
python train.py --dataset_id 0 --split_id 0 --model_id 0

# 自定义训练参数
python train.py --lr 1e-4 --epochs 3 --batch_size 2 --gradient_accumulation 4
```

- **涉及文件**：`train.py`
- **参数说明**：
  - `--model_path`: LLM模型路径（默认`./models/qwen2.5-1.5b`）
  - `--model_id`: 模型ID（默认自动递增）
  - `--dataset_id`: 数据集ID（默认0）
  - `--split_id`: 划分ID（默认0）
  - `--lr`: 学习率（默认1e-4）
  - `--epochs`: 训练轮数（默认3）
  - `--batch_size`: 每个设备的batch大小（默认2）
  - `--gradient_accumulation`: 梯度累积步数（默认4）
- **输出结果**：`saved_models/model_X/`
  - `pytorch_model.bin`（模型参数）
  - `config.txt`（训练配置）
  - `loss_logs/loss_log.csv`（训练loss记录，用于画loss曲线）
  - `loss_logs/loss_log.json`（loss记录JSON格式）
- **日志记录**：自动生成 `logs/training/log_X.json`

### 7. 模型测试

在测试集上评估模型性能：

```bash
# 使用模型ID测试
python test_model.py --model_id 0 --dataset_id 0 --split_id 0

# 使用路径测试
python test_model.py --saved_model_path ./saved_models/model_0 --dataset_id 0 --split_id 0
```

- **涉及文件**：`test_model.py`
- **参数说明**：
  - `--model_id`: 模型ID（与`--saved_model_path`二选一）
  - `--saved_model_path`: 模型路径（与`--model_id`二选一）
  - `--dataset_id`: 数据集ID（默认0）
  - `--split_id`: 划分ID（默认0）
  - `--no_save_report`: 不保存测试报告
- **输出结果**：
  - 控制台打印评估指标（准确率、精确率、召回率、F1值）
  - `test_reports/report_X.json`（测试报告JSON）
  - `test_reports/reports_index.csv`（报告索引CSV）
- **返回值**：包含评估指标的字典

---

## 完整流程示例

```bash
# 1. 数据清洗
python data_cleaning.py

# 2. 提取子集（自动递增ID，得到dataset_0）
python extract_subset.py --num_samples 5000

# 3. 模态分离（自动递增split_id，得到split_0）
python split_modality.py --dataset_id 0

# 4. 训练模型（自动递增model_id，得到model_0）
python train.py --dataset_id 0 --split_id 0

# 5. 测试模型
python test_model.py --model_id 0 --dataset_id 0 --split_id 0
```

---

## 注意事项

### 硬件要求

- **GPU**：推荐 NVIDIA GPU（Ampere架构及以上），支持bfloat16混合精度训练
- **显存**：至少16GB（训练Qwen2.5-1.5B模型）
- **内存**：至少16GB（处理大数据集）

### 数据格式

- 原始数据集：CSV格式，包含`Label`列（值为`BENIGN`或`DDoS`）
- 支持的特征列：`Destination Port`, `Bwd Packet Length Mean`, `Avg Bwd Segment Size`, `Bwd Packet Length Max`, `Bwd Packet Length Std`, `URG Flag Count`, `Packet Length Mean`, `Average Packet Size`, `Packet Length Std`

### 日志系统

所有操作都会自动生成日志文件：

| 日志类型 | 保存位置 | 内容 |
|---------|---------|------|
| 子集提取 | `logs/subset/log_X.json` | 时间、数据集ID、样本数、正负样本数 |
| 数据划分 | `logs/split/log_X.json` | 时间、数据集ID、划分ID、训练/测试样本数 |
| 模型训练 | `logs/training/log_X.json` | 时间、模型ID、数据集ID、训练参数、耗时 |
| 模型测试 | `test_reports/report_X.json` | 时间、模型ID、评估指标、混淆矩阵 |

### 测试脚本

运行项目测试套件验证所有功能：

```bash
python -m pytest test_project.py -v
```

---

## 文件结构

```
多模态融合2/
├── data_cleaning.py          # 数据清洗
├── extract_subset.py         # 子集提取
├── split_modality.py         # 模态分离
├── train.py                  # 模型训练
├── test_model.py             # 模型测试
├── test_project.py           # 项目测试
├── download_bert.py          # BERT下载
├── download_qwen.py          # Qwen下载
│
├── data_processing/          # 原始数据
│   └── *.csv
│
├── processed_dataset/        # 处理后数据
│   ├── processed_dataset.csv
│   └── dataset_X.csv
│
├── split_data/               # 划分后数据
│   └── dataset_X/split_Y/
│       ├── train.npz
│       ├── test.npz
│       └── ...
│
├── saved_models/             # 训练模型
│   └── model_X/
│       ├── pytorch_model.bin
│       ├── config.txt
│       └── loss_logs/
│
├── models/                   # 预训练模型
│   ├── bert/
│   └── qwen2.5-1.5b/
│
├── logs/                     # 操作日志
│   ├── subset/
│   ├── split/
│   └── training/
│
├── test_reports/             # 测试报告
│   ├── reports_index.csv
│   └── report_X.json
│
└── src/                      # 核心源码
    ├── data/
    │   └── data_loader.py
    └── model_architectures/
        ├── bert_encoder.py
        ├── numeric_encoder.py
        ├── fusion_projection.py
        └── multi_modal_model.py
```